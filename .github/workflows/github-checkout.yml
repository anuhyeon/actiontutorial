name: github checkout

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2 # 현재 우리의 저장소를 클론하고 체크아웃한 상태에서 다음 명령어를 실행 할 수 있는 상태를 만들어줌.
    - name: Run pwd
      run: pwd
    - name: Run ls -al
      run: ls -al
